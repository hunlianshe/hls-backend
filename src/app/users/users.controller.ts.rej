diff a/src/app/users/users.controller.ts b/src/app/users/users.controller.ts	(rejected hunks)
@@ -486 +486,4 @@ msg:""
-    return await this.userService.listLikes(body.type, req.user.openid||body.openid)
+    return await this.userService.listLikes(
+      body.type,
+      req.user.openid || body.openid,
+    )
